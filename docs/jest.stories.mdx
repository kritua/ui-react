import { Meta } from '@storybook/addon-docs';

<Meta
    title="Jest"
    parameters={{
        backgrounds: {
            disable: true,
            grid: {
                disable: true
            }
        },
        viewMode: 'docs',
        previewTabs: {
            canvas: {
                hidden: true
            }
        }
    }}
/>

## JEST
Установка необходимых пакетов
```
npm i ts-jest -S
```

Конфигурация JEST
```js
module.exports = {
    // ...
    transformIgnorePatterns: ['/node_modules/(?!@kritua/ui-react/)'],
    transform              : {
        // ...
        '@kritua/ui-react.+\.tsx?$': 'ts-jest'
    }
};
```
